// Original content before the duplicate line was removed
// ... other lines of code

// Updated -  Keeping everything as is, removing duplicate `const plant = res?.data;` line

const plant = res?.data; // Original line

// ... rest of the code unless specified otherwise
